__pycache__/
.venv/
*.pyc

node_modules/

# Local Ganache deploy output (address changes when Ganache restarts)
contracts/deployments/localhost.json

# Hardhat — cache embeds absolute filesystem paths; never commit
contracts/cache/

data/
outputs/
plots/
*.csv

# Secrets and environment (tracked examples may be negated below)
.env
.env.*
!.env.example
*.pem
*.p12
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.log

# Phase docs (planning only, not part of final code)
docs/phases/
